# Additional dependencies for the PubMed ingestion pipeline.
# Install in addition to the root requirements.txt:
#   uv pip install -r requirements.txt -r ingestion/requirements.txt

# PubMed XML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Embeddings (same model as the rest of the project)
sentence-transformers>=3.0.0

# Neo4j ADK integration (read-only graph queries)
# Provides: from neo4j_for_adk import graphdb
neo4j-for-adk>=0.1.0

# Neo4j driver (write operations — Chunk nodes + MENTIONS edges)
neo4j>=5.0.0
